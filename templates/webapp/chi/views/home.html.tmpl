{{define "content"}}
<div class="home-page">
    <h2>{{.Title}}</h2>
    <p>{{.Message}}</p>
    <div class="features">
        <div class="feature">
            <h3>Chi Router</h3>
            <p>Lightweight, fast, and expressive HTTP router for Go.</p>
        </div>
        <div class="feature">
            <h3>Clean Architecture</h3>
            <p>Built with separation of concerns and maintainability in mind.</p>
        </div>
        <div class="feature">
            <h3>Ready to Go</h3>
            <p>Start building your application right away without boilerplate hassle.</p>
        </div>
    </div>
</div>
{{end}}
