{{define "content"}}
<div class="home-page">
    <h2>{{.Title}}</h2>
    <p>{{.Message}}</p>
    <div class="features">
        <div class="feature">
            <h3>Standard Library</h3>
            <p>Built using Go's standard net/http package for maximum compatibility and performance.</p>
        </div>
        <div class="feature">
            <h3>Simple Design</h3>
            <p>Clean, organized code structure for ease of maintenance.</p>
        </div>
        <div class="feature">
            <h3>No Dependencies</h3>
            <p>Minimal external dependencies for a lightweight application.</p>
        </div>
    </div>
</div>
{{end}}
